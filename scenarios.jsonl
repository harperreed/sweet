{"name": "local-sync-e2e", "description": "End-to-end sync test against local syncvaultd server", "given": ["syncvaultd running on localhost:8090", "Fresh SSH keypair generated", "Fresh vault keys from seed"], "when": ["Register device with SSH pubkey", "Request challenge", "Sign challenge and verify", "Push encrypted change", "Pull changes back"], "then": ["Auth token received", "Change stored in server", "Decrypted data matches original"], "validates": ["SSH auth flow", "Encrypted sync round-trip", "PocketBase integration"]}
{"name": "multi-device-sync", "description": "Verify second device can see changes from first device", "given": ["First device authenticated and pushed data", "Second SSH keypair generated"], "when": ["Second device registers with same user_id", "Second device pulls changes"], "then": ["Second device sees first device's changes", "Both devices remain authenticated"], "validates": ["Multi-device support", "Device isolation", "Change visibility across devices"]}
